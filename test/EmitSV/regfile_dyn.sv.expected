// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module regfile_dyn (
    input  logic clk,
    input  logic rst_n,
    input  logic [7:0] waddr,
    input  logic signed [15:0] wdata,
    input  logic we,
    input  logic [7:0] raddr,
    input  logic reset,
    output logic signed [15:0] rdata
);

    logic signed [31:0] v0_1;
    logic signed [31:0] v1_1;
    logic signed [15:0] regs [4];
    logic signed [15:0] regs_next [4];

    always_comb begin
        v0_1 = '0;
        regs_next[0] = regs[0];
        regs_next[2] = regs[2];
        regs_next[1] = regs[1];
        regs_next[3] = regs[3];
        if (reset) begin
            regs_next[0] = 16'sd0;
            regs_next[1] = 16'sd0;
            regs_next[2] = 16'sd0;
            regs_next[3] = 16'sd0;
        end
        if (we) begin
            v0_1 = 32'(waddr);
            regs_next[0] = ((v0_1 == 32'sd0) ? wdata : 16'(regs[0]));
            regs_next[1] = ((v0_1 == 32'sd1) ? wdata : 16'(regs[1]));
            regs_next[2] = ((v0_1 == 32'sd2) ? wdata : 16'(regs[2]));
            regs_next[3] = ((v0_1 == 32'sd3) ? wdata : 16'(regs[3]));
        end
        v1_1 = 32'(raddr);
        rdata = (v1_1 == 32'sd0) ? 16'(regs[0]) : ((v1_1 == 32'sd1) ? 16'(regs[1]) : ((v1_1 == 32'sd2) ? 16'(regs[2]) : 16'(regs[3])));
    end

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            regs[0] <= 16'sd0;
            regs[2] <= 16'sd0;
            regs[1] <= 16'sd0;
            regs[3] <= 16'sd0;
        end else begin
            regs[0] <= regs_next[0];
            regs[2] <= regs_next[2];
            regs[1] <= regs_next[1];
            regs[3] <= regs_next[3];
        end
    end

endmodule
