// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module popcount (
    input  logic [15:0] x,
    output logic signed [7:0] n
);

    always_comb begin
        // Population count (Hamming weight) of a 16-bit input. Sums
        // each individual bit. The MATLAB source uses bitand + shift
        // at constant positions; the SV emitter renders as a chain of
        // conditional adds that synthesizes to a balanced adder tree.
        // Tests:
        // - bit-extraction via `bitand(bitshift(x, -K), 1)` with
        // constant K (post-bitshift-lowering fix)
        // - 16-element accumulator pattern, all i8-typed, into a
        // 5-bit-wide output (max value 16)
        // - integer-typed initial accumulator value followed by
        // conditional adds that all flow into the output
        n = 8'sd0;
        if ((x & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd1) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd2) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd3) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd4) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd5) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd6) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd7) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd8) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd9) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd10) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd11) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd12) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd13) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd14) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
        if (((x >> 16'sd15) & 16'sd1) != 0) begin
            n = n + 8'sd1;
        end
    end

endmodule
