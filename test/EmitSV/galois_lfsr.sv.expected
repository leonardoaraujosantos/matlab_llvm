// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module galois_lfsr (
    input  logic clk,
    input  logic rst_n,
    input  logic en,
    input  logic reset,
    output logic [15:0] rand_out
);

    logic signed [15:0] s;
    logic [15:0] state;
    logic [15:0] state_next;

    always_comb begin
        s = '0;
        state_next = state;
        if (reset) begin
            // seed; all-zero state would be a fixed point
            state_next = 16'sd1;
        end
        if (en) begin
            // Snapshot to break through the runtime f64 ABI.
            s = state + 16'sd0;
            // Galois LFSR: shift right one bit; if the LSB was 1, XOR
            // the polynomial mask into the shifted state.
            if ((s & 16'sd1) != 0) begin
                state_next = ((s >> 16'sd1) ^ -16'sd19456);
            end else begin
                state_next = (s >> 16'sd1);
            end
        end
        rand_out = state;
    end

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            state <= 16'sd1;
        end else begin
            state <= state_next;
        end
    end

endmodule
