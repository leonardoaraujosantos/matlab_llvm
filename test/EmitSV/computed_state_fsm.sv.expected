// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module computed_state_fsm (
    input  logic clk,
    input  logic rst_n,
    input  logic advance,
    input  logic jump,
    input  logic reset,
    output logic active,
    output logic [7:0] step_out
);

    logic [7:0] step;
    logic [7:0] step_next;

    always_comb begin
        step_next = step;
        if (reset) begin
            step_next = 8'sd0;
        end
        if (jump) begin
            step_next = 8'sd0;
        end else begin
            if (advance) begin
                if (step == 7) begin
                    step_next = 8'sd0;
                end else begin
                    step_next = step + 8'sd1;
                end
            end
        end
        active = step != 0;
        step_out = step;
    end

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            step <= 8'sd0;
        end else begin
            step <= step_next;
        end
    end

endmodule
