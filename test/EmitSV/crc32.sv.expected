// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module crc32 (
    input  logic clk,
    input  logic rst_n,
    input  logic data_in,
    input  logic en,
    input  logic reset,
    output logic signed [31:0] crc
);

    logic signed [31:0] cur;
    logic signed [31:0] data_in_u32;
    logic signed [31:0] feedback;
    logic signed [31:0] msb;
    logic signed [31:0] shifted;
    logic signed [31:0] crc_reg;
    logic signed [31:0] crc_reg_next;

    always_comb begin
        cur = '0;
        data_in_u32 = '0;
        feedback = '0;
        msb = '0;
        shifted = '0;
        crc_reg_next = crc_reg;
        if (reset) begin
            // all-zeros init (CRC residual depends
            crc_reg_next = 32'sd0;
        end
        if (en) begin
            cur = crc_reg + 32'sd0;
            // Extract MSB of cur (bit 31).
            msb = (cur >> 32'sd31) & 32'sd1;
            // Boolean → u32 conversion via branch.
            data_in_u32 = 32'sd0;
            if (data_in) begin
                data_in_u32 = 32'sd1;
            end
            feedback = msb ^ data_in_u32;
            // crc << 1, then XOR poly when feedback was 1.
            shifted = cur << 32'sd1;
            crc_reg_next = (shifted ^ (feedback * 32'sd79764919));
        end
        crc = crc_reg;
    end

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            crc_reg <= 32'sd0;
        end else begin
            crc_reg <= crc_reg_next;
        end
    end

endmodule
