// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module mealy_canon (
    input  logic clk,
    input  logic rst_n,
    input  logic [7:0] input_bit,
    input  logic [7:0] reset,
    output logic out_signal
);

    typedef enum logic {S0, S1} current_state_t;

    current_state_t current_state;
    current_state_t current_state_next;

    always_comb begin
        current_state_next = current_state;
        if (reset != 8'sd0) begin
            current_state_next = S0;
        end
        out_signal = 1'b0;
        unique case (current_state)
            S0: begin
                if (input_bit == 8'sd1) begin
                    current_state_next = S1;
                end
            end
            S1: begin
                if (input_bit == 8'sd1) begin
                    out_signal = 1'b1;
                end else begin
                    current_state_next = S0;
                end
            end
            default: begin
                current_state_next = S0;
            end
        endcase
    end

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            current_state <= S0;
        end else begin
            current_state <= current_state_next;
        end
    end

endmodule
