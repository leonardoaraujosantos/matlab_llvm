// Generated by matlabc -emit-systemverilog. Do not edit.
// Target: ASIC, vendor-neutral synthesizable SystemVerilog.
// Reset convention: async-low (assert async, deassert sync).

module hier_sequential (
    input  logic clk,
    input  logic rst_n,
    input  logic en,
    input  logic reset,
    output logic signed [7:0] c1,
    output logic signed [7:0] c2
);

    logic signed [7:0] u_counter1_0_n;
    logic signed [7:0] u_counter1_1_n;

    counter1 u_counter1_0 (
        .clk(clk),
        .rst_n(rst_n),
        .en(en),
        .reset(reset),
        .n(u_counter1_0_n)
    );
    counter1 u_counter1_1 (
        .clk(clk),
        .rst_n(rst_n),
        .en(en),
        .reset(reset),
        .n(u_counter1_1_n)
    );

    always_comb begin
        // cocotb: stimulus(reset, constant, 0)
        // Hierarchical multi-module with a sequential helper. The top
        // instantiates two counter modules; clk + rst_n are auto-added
        // to the top because at least one callee (counter1) has
        // persistent state, and the instantiation wires both through.
        c1 = u_counter1_0_n;
        c2 = u_counter1_1_n;
    end

endmodule

module counter1 (
    input  logic clk,
    input  logic rst_n,
    input  logic en,
    input  logic reset,
    output logic [7:0] n
);

    logic [7:0] n_reg;
    logic [7:0] n_reg_next;

    always_comb begin
        n_reg_next = n_reg;
        if (reset) begin
            n_reg_next = 8'sd0;
        end
        if (en) begin
            n_reg_next = n_reg + 8'sd1;
        end
        n = n_reg;
    end

    always_ff @(posedge clk or negedge rst_n) begin
        if (!rst_n) begin
            n_reg <= 8'sd0;
        end else begin
            n_reg <= n_reg_next;
        end
    end

endmodule
