// Generated by matlabc -emit-c. Do not edit.
#include <stdint.h>
#include <stdbool.h>

// Runtime prototypes (linked against runtime/matlab_runtime.c).
extern void matlab_disp_f64(double);
extern void matlab_parfor_dispatch(double, double, double, void*, void*);

// Forward declarations.
static void __parfor_body_0(double, void*);

// Program with parfor — disp substitutions must keep the runtime call
// so the mutex still coordinates interleaved output between workers.
int main(void) {
  matlab_parfor_dispatch(1.0, 1.0, 3.0, (void*)__parfor_body_0, 0);
  return 0;
}

// Program with parfor — disp substitutions must keep the runtime call
// so the mutex still coordinates interleaved output between workers.
static void __parfor_body_0(double v0, void* v1) {
  matlab_disp_f64(v0);
}

