% File-top doc comment describing this program.
% Multi-line block — each line should appear as a `//` line in output.

% Another paragraph, separated from the first by a blank line.
disp('hello');        % trailing comment after a string disp
disp(42);             % trailing on numeric disp

% Leading comment for the next statement.
x = 1;                % inline trailing with number

disp(x);              % prints the variable
