// Generated by matlabc -emit-c. Do not edit.
#include <stdint.h>
#include <iostream>

// Runtime prototypes (linked against runtime/matlab_runtime.c).
extern "C" {
} // extern "C"

// Module-level string constants.

// Forward declarations.
static double rec(double);

// Else arm contains a call — can't hoist into a ternary.
// Keeps the `double y = 0; if (...) { y = …; } else { y = …; }` shape.
int main(void) {
  auto v0 = rec(5);
  std::cout << v0 << '\n';
  return 0;
}

static double rec(double n) {
  double y = 0;
  if (n <= 1) {
    y = 1;
  } else {
    auto v1 = rec(n - 1);
    y = n * v1;
  }
  return y;
}

