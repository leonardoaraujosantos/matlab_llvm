emit-python (numpy) diverges from the C-runtime golden by more than the numeric tolerance; the Python port is best-effort (matches the existing .skip-emit-python precedent for the control/stats lanes).
