% Auto-generated by matlabc — DO NOT EDIT.
% Chart: blink
%
% State codes (1-based; 0 = uninitialised):
%   1 = off
%   2 = on

% --- Demo driver: 5 ticks. Replace with your own driver. ---
for k = 1:5
  disp(blink_tick(false));
end

function out_led = blink_tick(ev_tick)
  persistent init_done;
  persistent tick_count;
  persistent r_chart_root;
  persistent t_off;
  persistent t_on;
  persistent l_led;
  if isempty(init_done)
    init_done = 0;
    tick_count = 0;
    r_chart_root = 0;
    t_off = 0;
    t_on = 0;
    l_led = 0;
  end
  if init_done == 0
    r_chart_root = 1;
    t_off = tick_count;
    l_led = 0;
    init_done = 1;
  end
  tick_count = tick_count + 1;
  fired = true;
  iter = 0;
  while fired
    if iter >= 1000
      fired = false;
    else
      iter = iter + 1;
      fired = false;
      if r_chart_root == 1
        if ~fired && (((tick_count - t_off) >= 3))
          r_chart_root = 2;
          t_on = tick_count;
          l_led = 1;
          fired = true;
        end
      end
      if r_chart_root == 2
        if ~fired && (((tick_count - t_on) >= 2))
          r_chart_root = 1;
          t_off = tick_count;
          l_led = 0;
          fired = true;
        end
      end
    end
  end
  out_led = l_led;
end
