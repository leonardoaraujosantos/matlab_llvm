% Auto-generated by matlabc — DO NOT EDIT.
% Chart: vending
%
% State codes (1-based; 0 = uninitialised):
%   1 = have
%   2 = idle

% --- Demo driver: 5 ticks. Replace with your own driver. ---
for k = 1:5
  disp(vending_tick(0, false, false));
end

function out_dispense = vending_tick(in_coin, ev_insertCoin, ev_select)
  persistent init_done;
  persistent r_chart_root;
  persistent l_coin;
  persistent l_credit;
  if isempty(init_done)
    init_done = 0;
    r_chart_root = 0;
    l_credit = 0;
    l_coin = 0;
  end
  l_coin = in_coin;
  if init_done == 0
    r_chart_root = 2;
    l_credit = 0;
    init_done = 1;
  end
  l_dispense = 0;
  fired = true;
  iter = 0;
  while fired
    if iter >= 1000
      fired = false;
    else
      iter = iter + 1;
      fired = false;
      if r_chart_root == 2
        if ~fired && ev_insertCoin
          l_credit = l_credit + 1;
          r_chart_root = 1;
          fired = true;
        end
      end
      if r_chart_root == 1
        if ~fired && ev_select && (l_credit>0)
          l_dispense = 1;
          r_chart_root = 2;
          l_credit = 0;
          fired = true;
        end
        if ~fired
          % wait for selection;
        end
      end
    end
  end
  out_dispense = l_dispense;
end
